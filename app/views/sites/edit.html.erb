<div class="form-container">
  <header>
    <%= link_to "← Back to Site", site_path(@site), class: "link link-bold" %>

<div class="page-header">
              <h1>Edit Site</h1>
      <p class="text-muted">Update monitoring settings for <%= @site.url %></p>
    </div>
  </header>

  <div>
    <%= render 'sites/partials/form', site: @site %>
  </div>
</div>