<div class="card">
  <h2 class="card-title">Site Details</h2>

  <div class="details">
    <div class="detail">
      <span class="detail-label">URL</span>
      <span class="detail-value">
        <%= link_to site.url, site.url, target: "_blank", rel: "noopener", class: "link" %>
      </span>
    </div>

    <div class="detail">
      <span class="detail-label">Status</span>
      <span class="detail-value">
        <span class="status-dot <%= site.active? ? 'success' : 'error' %>"></span>
        <%= site.active? ? 'Monitoring Active' : 'Monitoring Paused' %>
      </span>
    </div>

    <div class="detail">
      <span class="detail-label">Check Frequency</span>
      <span class="detail-value"><%= pluralize(site.frequency_seconds, 'second') %></span>
    </div>

    <% if site.timezone_offset_hours && site.timezone_offset_hours != 0 %>
      <div class="detail">
        <span class="detail-label">Timezone Offset</span>
        <span class="detail-value">
          <%= site.timezone_offset_hours > 0 ? '+' : '' %><%= site.timezone_offset_hours %> hours
        </span>
      </div>
    <% end %>

    <div class="detail">
      <span class="detail-label">Created</span>
      <span class="detail-value"><%= site.created_at.strftime("%B %d, %Y at %l:%M %p") %></span>
    </div>

    <% if site.updated_at != site.created_at %>
      <div class="detail">
        <span class="detail-label">Last Updated</span>
        <span class="detail-value"><%= site.updated_at.strftime("%B %d, %Y at %l:%M %p") %></span>
      </div>
    <% end %>
  </div>
</div>