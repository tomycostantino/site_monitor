<div class="card">
  <h2 class="card-title">Actions</h2>

  <div class="flex-col-md">
    <%= link_to "✏️ Edit Site", edit_site_path(site), class: "btn btn-primary btn-full" %>

    <button class="btn btn-secondary btn-full" onclick="checkSiteNow()">
      🔍 Check Now
    </button>

    <% if site.active? %>
      <button class="btn btn-warning btn-full" onclick="pauseMonitoring()">
        ⏸️ Pause Monitoring
      </button>
    <% else %>
      <button class="btn btn-success btn-full" onclick="resumeMonitoring()">
        ▶️ Resume Monitoring
      </button>
    <% end %>

    <%= link_to "🗑️ Delete Site", site_path(site), method: :delete,
                confirm: "Are you sure you want to delete this site? This action cannot be undone.",
                class: "btn btn-danger btn-full" %>
  </div>
</div>