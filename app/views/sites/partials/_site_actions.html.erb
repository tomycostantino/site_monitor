<div class="site-actions-card">
  <h2 class="card-title">Actions</h2>

  <div class="actions-list">
    <%= link_to edit_site_path(site), class: "action-btn primary" do %>
      <span class="action-icon">✏️</span>
      <span class="action-text">Edit Site</span>
    <% end %>

    <button class="action-btn secondary" onclick="checkSiteNow()">
      <span class="action-icon">🔍</span>
      <span class="action-text">Check Now</span>
    </button>

    <% if site.active? %>
      <button class="action-btn warning" onclick="pauseMonitoring()">
        <span class="action-icon">⏸️</span>
        <span class="action-text">Pause Monitoring</span>
      </button>
    <% else %>
      <button class="action-btn success" onclick="resumeMonitoring()">
        <span class="action-icon">▶️</span>
        <span class="action-text">Resume Monitoring</span>
      </button>
    <% end %>

    <%= link_to site_path(site), method: :delete,
                confirm: "Are you sure you want to delete this site? This action cannot be undone.",
                class: "action-btn danger" do %>
      <span class="action-icon">🗑️</span>
      <span class="action-text">Delete Site</span>
    <% end %>
  </div>
</div>